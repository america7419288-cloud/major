/**
 * KanbanBoard — thin wrapper around TaskBoardView.
 * Kept for backward-compatibility if referenced from other places.
 * TasksPage.tsx now uses TaskBoardView directly.
 */
export { default as KanbanBoard } from './TaskBoardView';
